package com.samrit.job.domain;

public enum ResumeVisibility {
    PUBLIC,   // discoverable by any employer in the talent pool
    PRIVATE,   // only visible when candidates actively applies
    LINK_ONLY // accessible only via a direct share link
}
